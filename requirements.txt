fastapi==0.115.0
uvicorn[standard]==0.30.0
sqlalchemy==2.0.35
alembic==1.13.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==4.0.1
python-multipart==0.0.9
httpx==0.27.0
python-dotenv==1.0.1
aiofiles==24.1.0
jinja2==3.1.4
pydantic[email]>=2.9.0
boto3==1.35.0
# F-077: PostgreSQL driver. psycopg3 (the `psycopg` package) over
# psycopg2 — modern, actively maintained, better typing. Used for
# both local dev (docker-compose Postgres) and production.
psycopg[binary]>=3.1
gunicorn==22.0.0
